一个简单的prompt函数
get_response:
创建一个message字符串
依次加上 提示信息 数据库中以‘成绩’为关键词读取的数据 当前的时间 之前的回复时间与内容 回复要求与样例
随后将字符串发送给ai，再由ai生成下一次回复用户的时间与内容
时间格式为 year-month-day xx:xx:xx 回复内容为字符串 以一个list的形式返回
在函数属性中加了个list来储存之前回复用户的内容并一起发给ai（因为数据库没有
（（（怎么感觉我的任务还挺没技术含量的，是我理解错了吗（（（虽然python水平有限还是写的一坨